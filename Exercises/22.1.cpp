#include <iostream>

int main()
{
	/* Write a program that defines two variables of type `int`
	with automatic storage duration (placed on the stack) inside
	the `main` function scope */

	int x = 2;
	int y = 4;
}